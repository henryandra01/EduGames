spring:
  datasource:
    url: jdbc:mysql://localhost:${DB_PORT:3307}/${DB_NAME:edugames_db}
    username: root
    password: ${DB_PASSWORD:@Erosenin1}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
